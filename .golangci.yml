linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - revive

run:
  timeout: 5m

linters-settings:
  goimports:
    local-prefixes: github.com/tdesaules/coredns-service-discovery
